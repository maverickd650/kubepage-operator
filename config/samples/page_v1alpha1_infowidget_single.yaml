apiVersion: page.kubepage.dev/v1alpha1
kind: InfoWidget
metadata:
  labels:
    app.kubernetes.io/name: kubepage-operator
    app.kubernetes.io/managed-by: kustomize
  name: infowidget-single-sample
spec:
  # The single-widget form: soft-deprecated in favor of the multi-widget form
  # (see page_v1alpha1_infowidget.yaml), kept here for regression coverage
  # only.
  dashboardRef:
    name: dashboard-sample
  type: greeting
  order: 1
  options:
    text: Welcome to the lab
