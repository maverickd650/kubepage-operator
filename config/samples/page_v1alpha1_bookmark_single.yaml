apiVersion: page.kubepage.dev/v1alpha1
kind: Bookmark
metadata:
  labels:
    app.kubernetes.io/name: kubepage-operator
    app.kubernetes.io/managed-by: kustomize
  name: bookmark-single-sample
spec:
  # The single-bookmark form: soft-deprecated in favor of the multi-bookmark
  # form (see page_v1alpha1_bookmark.yaml), kept here for regression coverage
  # only.
  dashboardRef:
    name: dashboard-sample
  group: Developer
  name: Github
  href: https://github.com/
  abbr: GH
