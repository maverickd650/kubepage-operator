{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "kubepage-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "kubepage-operator.resourceName" (dict "suffix" "dashboardstyle-viewer-role" "context" $) }}
rules:
- apiGroups:
  - page.kubepage.dev
  resources:
  - dashboardstyles
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - page.kubepage.dev
  resources:
  - dashboardstyles/status
  verbs:
  - get
{{- end }}
